In `config.typ` kann man Parameter einstellen, wie Sprache, Name, Abstract ja nein, etc.

In `content/content.typ` schreibt man den Inhalt rein.

In `content/abstract.typ` schreibt man seinen Abstract.

In `util.typ` findet man Hilfsfunktionen.